--- a/python/pypto/ir/distributed_compiled_program.py
+++ b/python/pypto/ir/distributed_compiled_program.py
@@ -497,6 +497,7 @@ class DistributedCompiledProgram:
         sub_worker_overrides: dict[str, Callable[..., Any]] | None = None,
         inherited_host_tensors: "Sequence[torch.Tensor | ReadOnlyHostTensor] | None" = None,
         startup_timeout_s: float | None = None,
+        chip_service_factories: dict[int, Callable[..., Any]] | None = None,
     ) -> "DistributedWorker":
         """Prepare a reusable L3 execution handle (setup once, dispatch many).
 
@@ -569,6 +570,10 @@ class DistributedCompiledProgram:
             startup_timeout_s: Optional positive finite bound, in seconds, for
                 the forked worker hierarchy to report startup readiness. ``None``
                 keeps Simpler's default timeout.
+            chip_service_factories: Optional rank-indexed owner-local service
+                factories. Each runs after chip initialization and must return
+                a service whose ``close`` method joins its threads before device
+                memory teardown.
 
         Returns:
             A :class:`DistributedWorker`; use it as a context manager or call
@@ -585,6 +590,7 @@ class DistributedCompiledProgram:
             sub_worker_overrides=sub_worker_overrides,
             inherited_host_tensors=inherited_host_tensors,
             startup_timeout_s=startup_timeout_s,
+            chip_service_factories=chip_service_factories,
         )
 
     @staticmethod
--- a/python/pypto/runtime/device_runner.py
+++ b/python/pypto/runtime/device_runner.py
@@ -134,12 +134,30 @@ def _kernel_cache_file(
     )
 
 
-def _clean_git_revision(repo_root: Path) -> tuple[bool, str | None]:
-    """Return ``(is_checkout, HEAD)`` when *repo_root* is a clean checkout.
+_RUNTIME_BINARY_INPUT_PATHS = (
+    "src",
+    "simpler_setup",
+    "CMakeLists.txt",
+    "pyproject.toml",
+    "pto_isa.pin",
+)
+
+
+def _clean_git_revision(
+    repo_root: Path,
+    *,
+    input_paths: tuple[str, ...] = (),
+) -> tuple[bool, str | None]:
+    """Return ``(is_checkout, HEAD)`` when relevant inputs are clean.
 
     ``is_checkout`` remains true for a dirty or unreadable checkout. Callers use
     that distinction to avoid falling back to packaged revision metadata that
     does not describe the source files which will actually be compiled.
+
+    ``input_paths`` narrows the dirty-tree check to files which participate in
+    the cached artifact.  This lets a source checkout carry Python-only runtime
+    control-plane changes without invalidating generated kernel/orchestration
+    binaries.  An empty tuple retains the conservative whole-checkout check.
     """
     if not (repo_root / ".git").exists():
         return False, None
@@ -152,8 +170,11 @@ def _clean_git_revision(repo_root: Path) -> tuple[bool, str | None]:
             timeout=5,
             check=False,
         )
+        status_command = ["git", "status", "--porcelain=v1", "--untracked-files=normal"]
+        if input_paths:
+            status_command.extend(("--", *input_paths))
         status_result = subprocess.run(
-            ["git", "status", "--porcelain=v1", "--untracked-files=normal"],
+            status_command,
             cwd=str(repo_root),
             capture_output=True,
             text=True,
@@ -178,7 +199,10 @@ def _runtime_revision(compiler: KernelCompiler) -> str | None:
     """Return the runtime source/build revision used by *compiler*."""
     project_root = getattr(compiler, "project_root", None)
     if project_root is not None:
-        is_checkout, revision = _clean_git_revision(Path(project_root))
+        is_checkout, revision = _clean_git_revision(
+            Path(project_root),
+            input_paths=_RUNTIME_BINARY_INPUT_PATHS,
+        )
         if is_checkout:
             return revision
 
--- a/python/pypto/runtime/distributed_runner.py
+++ b/python/pypto/runtime/distributed_runner.py
@@ -532,6 +532,7 @@ def _construct_worker(
     num_sub: int,
     enable_sdma: bool = False,
     startup_timeout_s: float | None = None,
+    chip_service_factories: dict[int, Callable[..., Any]] | None = None,
 ) -> Any:
     """Construct a simpler ``Worker(level=3)`` from the distributed config."""
     if startup_timeout_s is not None and (not math.isfinite(startup_timeout_s) or startup_timeout_s <= 0):
@@ -554,6 +555,8 @@ def _construct_worker(
     }
     if startup_timeout_s is not None:
         worker_config["startup_timeout_s"] = startup_timeout_s
+    if chip_service_factories:
+        worker_config["chip_service_factories"] = chip_service_factories
     return Worker(**worker_config)
 
 
@@ -1837,6 +1840,7 @@ class DistributedWorker(Worker):
         sub_worker_overrides: dict[str, Callable[..., Any]] | None = None,
         inherited_host_tensors: Sequence[torch.Tensor | ReadOnlyHostTensor] | None = None,
         startup_timeout_s: float | None = None,
+        chip_service_factories: dict[int, Callable[..., Any]] | None = None,
     ) -> None:
         super().__init__()  # initialize Worker ABC state (_owned_tensors)
         # Simpler owns allocations as Buffer objects. PyPTO continues exposing
@@ -1976,6 +1980,7 @@ class DistributedWorker(Worker):
                 num_sub,
                 enable_sdma=enable_sdma,
                 startup_timeout_s=startup_timeout_s,
+                chip_service_factories=chip_service_factories,
             )
             from .tensor_arg import bind_tensor_arg_owner  # noqa: PLC0415
 
@@ -2516,6 +2521,18 @@ class DistributedWorker(Worker):
         self._w.free(handle)
         del self._device_buffers[(worker_id, ptr)]
 
+    def retain_service_tensor(self, tensor: DeviceTensor, worker_id: int) -> tuple[str, Any]:
+        """Retain a resident tensor for an owner-local service."""
+        self._require_open("retain_service_tensor")
+        if tensor.buffer is None:
+            raise ValueError("service tensor must retain its allocation Buffer")
+        return self._w.retain_service_buffer(tensor.buffer, worker_id)
+
+    def release_service_tensor(self, token: str) -> None:
+        """Release a service retention after confirmed native unregistration."""
+        self._require_open("release_service_tensor")
+        self._w.release_service_buffer(token)
+
     def committed_device_memory(self, worker_id: int = 0) -> int:
         """Total device HBM (bytes) committed by chip *worker_id*'s ``MemoryAllocator``
         (tensors + pooled arenas + runtime buffers). Routes through the underlying
